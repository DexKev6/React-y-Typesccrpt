//POner aqui las interfaces para no estar repitiendo por todo el codigo a lo pendejo 
/*aqui solo van definiciones (interfaces, typos etc) nada de funciones ni ndad asi
eso significa el .d*/ 
export interface Sub{
    nick: string,
    subMonths: number,
    avatar: string,
    description?: string //Opcional
  }